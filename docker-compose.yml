services:
  backend:
    build: ./backend
    container_name: ipb-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: ipb-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
